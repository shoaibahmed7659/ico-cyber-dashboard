# ICO Data Breach Intelligence — pinned dependencies
#   streamlit >= 1.50   -> width="stretch", st.fragment, st.segmented_control, st.popover
#   scikit-learn >= 1.3 -> OneHotEncoder(sparse_output), permutation_importance, class_weight
#   pandas >= 2.0       -> named aggregation, GroupBy.sample, stable Categorical/groupby
#   plotly >= 5.18      -> imshow(text_auto), density_heatmap, make_subplots, templates
#   numpy >= 1.24       -> compatible with the above
#   google-genai        -> the assistant (current unified Gemini SDK; replaces google-generativeai)

streamlit>=1.50
pandas>=2.0
numpy>=1.24
plotly>=5.18
scikit-learn>=1.3
google-genai>=1.0
