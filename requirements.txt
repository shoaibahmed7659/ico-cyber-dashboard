# ICO Data Breach Intelligence — pinned dependencies
# Lower bounds guarantee the APIs the app relies on while letting Streamlit
# Community Cloud install the current stable build.
#
#   streamlit >= 1.50   -> width="stretch" API + st.fragment (isolated reruns)
#   scikit-learn >= 1.3 -> OneHotEncoder(sparse_output), permutation_importance, class_weight
#   pandas >= 2.0       -> named aggregation, stable Categorical/groupby
#   plotly >= 5.18      -> imshow(text_auto), density_heatmap, make_subplots, registered templates
#   numpy >= 1.24       -> compatible with the above

streamlit>=1.50
pandas>=2.0
numpy>=1.24
plotly>=5.18
scikit-learn>=1.3
