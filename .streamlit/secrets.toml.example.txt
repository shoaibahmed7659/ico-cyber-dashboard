# Copy this file to ".streamlit/secrets.toml" and paste your own key.
# NEVER commit the real secrets.toml — add it to .gitignore.
GEMINI_API_KEY = "key"
# Optional: override the model if the default is unavailable on your key
# GEMINI_MODEL = "gemini-2.5-flash"
